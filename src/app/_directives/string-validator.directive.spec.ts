import { StringValidatorDirective } from './string-validator.directive';

describe('StringValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new StringValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
